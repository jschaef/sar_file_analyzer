#!/usr/bin/python3

import streamlit as st

def todo():
    st.markdown(
        "* implement a new page for displaying data at a given point in time :heavy_check_mark:")
    st.markdown("* replace compare different metrics by multiselection \
            to display arbitrary metrics :heavy_check_mark:")
    st.markdown("* comment functions")
    st.markdown("* put further code into functions")
    st.markdown("* move some functions into other files")
    st.markdown("* create a help/info page :heavy_check_mark:")
    st.markdown("* implement INTRR/s")
    st.markdown("* catch error if df contains only one measure\
        min, max value calculation throws an error in this case")
    st.markdown("* create PDF's from diagram :heavy_check_mark:")
    st.markdown("* create separate overview for cpu interface and blockdevice metrics")
    st.markdown("* For the latter check performance. If slow create separate pickle data")
    st.markdown("* hide pdf directory from show Sar Files, Analyze data, \
        compare different metrics, detailed view :heavy_check_mark:")
    st.markdown("* Display who is currently logged in")
    st.markdown('* Enhance "Update Header" functionality :heavy_check_mark:')
    st.markdown("* Implement password change :heavy_check_mark:")
    st.markdown("* Implement display reboot in dia and data frames:heavy_check_mark:")
    st.markdown("* Enhance multiple files view by specifying start end times :heavy_check_mark:")
    st.markdown("* Implement PDF download for multiple charts on summary page")
    st.markdown("* Looking again into threading :heavy_check_mark:")
